# Python dependencies for src/services/crawl_agent.py (the Business DNA
# website crawler, invoked as a subprocess from src/services/dna.service.ts).
# Installed into a .venv/ at the repo root by the Dockerfile - dna.service.ts
# hardcodes that path (`.venv/bin/python`), so keep it there if you install
# manually outside Docker too. Pinned to the versions verified working
# together locally.
Crawl4AI==0.9.2
beautifulsoup4==4.15.0
pillow==12.3.0
# 2.5.2 (the version pinned locally, where Python is 3.13) requires Python
# >=3.12; Debian bookworm's default python3 is 3.11, whose newest compatible
# numpy 2.x release is 2.4.6.
numpy==2.4.6
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
playwright==1.62.0
playwright-stealth==2.0.3
lxml==6.1.1
pydantic==2.13.4
